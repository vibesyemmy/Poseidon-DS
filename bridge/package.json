{
  "name": "@poseidon/bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local Node bridge between the Poseidon Figma plugin and Claude (via Claude Code auth).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "poseidon": "tsx watch src/server.ts",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.144",
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.12",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
